server:
  port: 8081

spring:
  application:
    name: agent

# MCP Server 配置
mcp:
  server:
    url: http://localhost:3000

# LLM 配置
# 请从环境变量 DASHSCOPE_API_KEY 或在此处配置 API Key
# 获取 API Key: https://dashscope.aliyun.com/
llm:
  api:
    key: sk-2f91a86b0eb74c2493830330169a847d
    model: qwen-plus

# 日志配置
logging:
  level:
    com.example.agent: DEBUG

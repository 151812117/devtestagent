# Solon 应用配置
server:
  port: 3000
  host: "0.0.0.0"

# 日志配置
solon.logging:
  level:
    root: INFO
    com.example.mcpserver: DEBUG
  appender:
    console:
      pattern: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
